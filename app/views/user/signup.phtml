<h1 class="title">Request website account</h1>
<?= l("back", GetReturnURL()); ?>
<?= GetErrorsFor($this->user); ?>
<?= FormTag("signup", \simp\Request::POST); ?>
<fieldset><legend>Account Information</legend>
<p>
Getting a website account is a two step process.  First, fill out this form
and submit it.  After you submit, an email will be sent to the address you provide
to verify you email address.  Follow the instructions in that email to confirm and
you will have an account!
</p>
<br />
<table class="form">
    <tr>
        <th class="label">Login</th>
        <td class="entry"><?= TextField($this->user, "login"); ?></td>
        <td class="help">This is your login id.  You will use it to login to the site.</td>
    </tr>
    <tr>
        <th class="label">Name (first last)</th>
        <td class="entry">
            <?= TextField($this->user, "first_name"); ?>
            <?= TextField($this->user, "last_name"); ?>
        </td>
        <td class="help">This is your name.</td>
    </tr>
    <tr>
        <th class="label">Password</th>
        <td class="entry"><?= PasswordField($this->user, "password"); ?></td>
        <td class="help">This will be your password.  Make it a good one.</td>
    </tr>
    <tr>
        <th class="label">Password Confirmation</th>
        <td class="entry"><?= PasswordField($this->user, "password_verification");?></td>
        <td class="help">Type your password again to make sure you didn't mess up the first time</td>
    </tr>
    <tr>
        <th class="label">Timezone</th>
        <td class="entry"><?= SimpleSelect($this->user, 'timezone', SupportedTimeZones());?></td>
        <td class="help">Select the timezone you are in so that date/time display is correct for you.</td>
    </tr>
    <tr>
        <th class="label">Email</th>
        <td class="entry"><?= TextField($this->user, 'email', array('size' => 40));?></td>
        <td class="help">This is your email address.</td>
    </tr>
    <tr>
        <th class="label">Send Email in HTML</th>
        <td class="entry"><?= CheckBoxField($this->user, 'email_html'); ?></td>
        <td class="help">Check this box if you want email to be sent in HTML format (pretty!)</td>
    </tr>
</table>
</fieldset>
<input type="submit" value="Request Account"/>
<?= EndForm(); ?> 
