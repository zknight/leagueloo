<h1>Create New User</h1>
<? $errors = $this->user->GetErrors();
   if (count($errors) > 0):
?>
<div class='errors'>
    <ul>
    <? foreach ($errors as $error): ?>
        <li><?= $error; ?></li>
    <? endforeach; ?>
    </ul>
</div>
<? endif; ?>
<?= FormTag("user", \simp\Request::POST) ?>
    <? require("_form.phtml"); ?>
    <input type="submit" value="Add"/>
<?= EndForm(); ?>
