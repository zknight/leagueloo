<ul class="menu">
    <? foreach ($this->GetPrograms() as $program): ?>
        <? if ($program->weight == 0): ?>
    <li><?= l("Home", Path::home()); ?></li>
        <? else: ?>
    <li><?= l($program->name, \Path::Relative(SnakeCase($program->name))); ?></li>
        <? endif; ?>
    <? endforeach; ?>
</ul>
